<Window x:Class="TESTdEM.Views.Windows.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TESTdEM.Views.Windows"
        mc:Ignorable="d"
                Title="Редактирование" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="20" />
            <RowDefinition Height="50" />
            <RowDefinition Height="20" />
            <RowDefinition Height="50" />
            <RowDefinition Height="20" />
            <RowDefinition Height="50" />

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Text="Название:"/>
        <TextBox Name="NameTb"  Grid.Row="0" Grid.Column="2" Text="{Binding Name}" />
        <TextBlock  Grid.Row="2" Text="Цена:"/>
        <TextBox Name="PriceTb" Grid.Row="2" Grid.Column="2" Text="{Binding Price}"/>
        <TextBlock  Grid.Row="4" Text="Описание:"/>
        <TextBox Name="DescriptionTb" Grid.Row="4" Grid.Column="2" Text="{Binding Description}" />
      
        <Button Grid.Row="6" Grid.Column="2" Name="Editbtn" Content="Редактировать" Click="Editbtn_Click" />
    </Grid>
</Window>
